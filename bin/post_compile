#!/usr/bin/env bash

echo "-----> Post-compile: Clearing staticfiles cache and rebuilding"
rm -rf $BUILD_DIR/staticfiles
rm -rf $BUILD_DIR/staticfiles.json
python $BUILD_DIR/manage.py collectstatic --noinput
echo "-----> Post-compile: Static files collected fresh"
